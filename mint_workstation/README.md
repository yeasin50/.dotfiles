# Linux Mint Workstation

Handles `.bashrc` and environments variables.

One `.bash_secrects` is kept ignore from here which includes

```sh
# ~/.bash_secrets

##  SSH  to my  potato
alias debian='ssh '

export WAKATIME_API_KEY=""
```

> Create a setup script

#### things are needed to be install

fzf, ripgrep, nvim, wezterm, kitty, tmux, lazygit

some other tools, like eassyeffect, kdenlive appImage setup + keymap, blender +keymap, audacity + keymap
