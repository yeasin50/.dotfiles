# ~/.bashrc generated by chezmoi

{{- if eq .chezmoi.os "linux" }}
# Linux specific logic
{{- if (hasPrefix "workstation" .chezmoi.hostname) }}
   # Your workstation specific sources
   [ -f "$HOME/.config/bash/mint_bashrc.bash" ] && source "$HOME/.config/bash/mint_bashrc.bash"
{{- end }}
{{- end }}

{{- if eq .chezmoi.os "darwin" }}
# macOS specific logic
{{- end }}

# Common sources for all machines
[ -f "$HOME/.config/bash/aliases.bash" ] && source "$HOME/.config/bash/aliases.bash"
